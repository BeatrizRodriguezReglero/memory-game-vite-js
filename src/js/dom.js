// Aquí van los getElementById y los querySelectorAll que pondrías al principio.
const gameBoardElement = document.getElementById('game-board');
const cards = document.querySelectorAll('.card');
const playAgainButtonElement = document.getElementById('button-play-again');
const resultsBoardElement = document.getElementById('results-board');

export { gameBoardElement, cards, playAgainButtonElement, resultsBoardElement };
